name: alh_calendar
description: A package for creating a custom calendar-widget.
  Builder-methods make it possible to design the calendar according to the given situation.
version: 1.2.0
repository: https://github.com/alhappler/alh_calendar

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  intl: ^0.19.0
